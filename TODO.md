# TODO

- [ ] **Impeccable review of the redesign.** Install the skill
  (`/plugin marketplace add pbakaus/impeccable`), then run `/impeccable critique src` and
  `/impeccable audit`. Goal: at least 30/40 (before the redesign: 20/40). Fix what it finds
  and update `DESIGN.md` if the system changes. The detector (`npx impeccable detect --json src`)
  found nothing on 2026-09-24.
